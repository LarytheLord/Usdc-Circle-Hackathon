const reportWebVitals = (onPerfEntry: any) => {
  if (onPerfEntry && typeof onPerfEntry === 'function') {
    // Simple implementation that doesn't require web-vitals package
    // This is a placeholder for the actual performance reporting
  }
};

export default reportWebVitals;